[env]
KUBECONFIG = "{{config_root}}/.kube/config"
_.path = "./scripts"

[tools]
go = "1.26.6"
golangci-lint = "2.12.2"
kubectl = "1.35"
helm = "3.19.0"
git-cliff = "2.13.1"
"go:golang.org/x/tools/gopls" = "0.23.0"
"go:golang.org/x/vuln/cmd/govulncheck" = "1.6.0"
"github:joshka/betamax" = "betamax-v0.1.15"
kind = "0.32.0"
# kwokctl = "0.8.0"
# cosign/syft (the release's signing/SBOM half) live in mise.release.toml,
# not here — see that file for why.
