package tui

// Version is kute's display version, shown on 10b's first-run wordmark
// (docs/design README.md §10b) and available for 7b's help-overlay version
// slot. No release process wires this to a real semver yet, so it stays a
// plain pre-1.0 placeholder rather than fabricating a specific release
// number.
const Version = "0.1.0-dev"
